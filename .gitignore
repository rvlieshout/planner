bin/
obj/
.vs/
.vscode/
.idea/
*.user
*.suo

# Local secrets and generated token-signing certificates
.env
keys/
*.pfx

# Build output: publish intermediates and packaged client releases
artifacts/

# Aspire's per-developer CLI state. aspire.config.json is checked in — it is what lets `aspire run`
# find the app host from the repository root — but nothing under this folder is.
.aspire/

# Test and coverage output
TestResults/
coverage/

App_Data/
